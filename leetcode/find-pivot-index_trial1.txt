class Solution:
    def pivotIndex(self, nums: List[int]) -> int:
        total_sum, left_sum = sum(nums), 0

        for i, n in enumerate(nums):
            if left_sum == (total_sum - n - left_sum):
                return i
            
            left_sum += n

        return -1